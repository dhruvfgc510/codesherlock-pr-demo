# Trigger reviews for PRs targeting these branches
target_branches: ["main", "develop"]

# Focus the review on a small, high-signal set of characteristics for this demo
preferred_characteristics: ["Code Injection", "Input Validation", "Modularity"]

# Show all severities, not just critical/high (default is critical + high)
severity_levels: ["critical", "high", "medium", "low"]
